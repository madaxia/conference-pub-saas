{
  "name": "conference-pub-saas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deps:up": "docker-compose -f infra/docker/docker-compose.yml up -d",
    "deps:down": "docker-compose -f infra/docker/docker-compose.yml down",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9.0.0"
  }
}
